(function () {
    angular
        .module("<%= name %>")
        .config(configuration);

    function configuration($routeProvider) {
        $routeProvider
            <% for (var entityName in entities) { %>
            .when("/<%= entityName %>", {
                templateUrl:"/wam/<%= entityName %>/templates/list/<%= entityName %>-list.view.client.html",
                controller:"<%= entityName %>ListController",
                controllerAs:"model"
                })
            .when("/<%= entityName %>/:Id", {
                template:"edit view"
            })

     <% }
        %>
    }
})();